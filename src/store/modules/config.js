const state = {
  mobile: false,
};

const getters = {

};

const actions = {

};

const mutations = {
  SET_MOBILE_USER: (state, user) => state.mobile = user,
};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
